import { FC } from "react";
import { MenuItemType } from "../../../../../application/@types/Menu";

interface MenuViewModalProps {
    isOpen: boolean
    selectedMenu: MenuItemType
    onClose: () => void
}

export const MenuViewModal:FC<MenuViewModalProps> = ({}) => {
    return <>
    
    </>
}